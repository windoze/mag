# 归档：mag-cli 计划（最小 CLI 验证原型 + 运行时配置系统）

本目录保存 mag **第二个 interface：`mag-cli`**（最小 CLI 验证原型）及其前置（pivot 能力、交互
origin 归因、运行时配置系统、delegation 接线、ask_user 工具）的**已完成**实施计划快照，全部里程碑
（M1–M6 + F-R 全计划 review）均已 `[DONE]`。此后进入第三个 interface：**mag-web**（单用户 web UI，
与 desktop 共用 UI 基础设施），其新计划见仓库根 `PLAN.md` / `TODO.md`，设计输入为
[`docs/WEB.md`](../../WEB.md)。

- [`PLAN.md`](PLAN.md)：mag-cli 的实施计划（唯一设计输入为 [`docs/CLI.md`](../../CLI.md)）。
- [`TODO.md`](TODO.md)：mag-cli 的逐任务清单，保留全部 `[DONE]` 完成记录作为历史。

设计源文档（仍在维护）：

- 全局设计：[`docs/DESIGN.md`](../../DESIGN.md)。
- CLI interface 与配置系统设计：[`docs/CLI.md`](../../CLI.md)。
- web/desktop UI 设计：[`docs/WEB.md`](../../WEB.md)。

更早的归档：[`../2026-07-19-mag-service/`](../2026-07-19-mag-service/)（service 主干）、
[`../2026-07-20-mag-acp/`](../2026-07-20-mag-acp/)（第一个 interface）。
