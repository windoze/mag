// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ConfigDto } from "./ConfigDto";
import type { InteractionResponseWire } from "./InteractionResponseWire";
import type { MessageAttachment } from "./MessageAttachment";
import type { RequestId } from "./RequestId";
import type { SessionConfig } from "./SessionConfig";
import type { SessionId } from "./SessionId";

/**
 * Commands accepted by the transport-neutral engine.
 */
export type Command = { "type": "create_session", 
/**
 * Session configuration to persist and use for the driver.
 */
config: SessionConfig, } | { "type": "list_sessions" } | { "type": "resume_session", 
/**
 * Session to resume.
 */
id: SessionId, } | { "type": "get_session_history", 
/**
 * Session whose history should be returned.
 */
id: SessionId, } | { "type": "delete_session", 
/**
 * Session to delete.
 */
id: SessionId, } | { "type": "send_message", 
/**
 * Session receiving the message.
 */
session_id: SessionId, 
/**
 * User-visible text payload.
 */
text: string, 
/**
 * Optional attachments supplied with the message.
 */
attachments?: Array<MessageAttachment>, } | { "type": "pivot_message", 
/**
 * Session receiving the pivot message.
 */
session_id: SessionId, 
/**
 * User-visible pivot text payload.
 */
text: string, } | { "type": "cancel_run", 
/**
 * Session whose active run should be cancelled.
 */
session_id: SessionId, } | { "type": "respond_interaction", 
/**
 * Session that owns the pending interaction.
 */
session_id: SessionId, 
/**
 * Request identity emitted by `InteractionRequested`.
 */
request_id: RequestId, 
/**
 * User or policy response to the interaction.
 */
response: InteractionResponseWire, } | { "type": "list_sources" } | { "type": "probe_local_agents" } | { "type": "get_config" } | { "type": "update_config", 
/**
 * Configuration DTO to validate and apply to the runtime config store.
 */
config: ConfigDto, } | { "type": "reload_config" } | { "type": "apply_config" };
